---
title: "List Cards"
api: "GET https://api.uncash.io/api/v1/cards"
description: "Get a paginated list of user's virtual cards"
---

## Query Parameters

<ParamField query="page" type="integer" default="1">
  Page number for pagination
</ParamField>

<ParamField query="limit" type="integer" default="10">
  Number of items per page (max: 50)
</ParamField>

## Response

<ResponseField name="success" type="boolean">
  Indicates if the request was successful
</ResponseField>

<ResponseField name="data" type="object">
  <Expandable title="properties">
    <ResponseField name="data" type="array">
      Array of user cards
      <Expandable title="Card object">
        <ResponseField name="id" type="integer">Card ID</ResponseField>
        <ResponseField name="card_nickname" type="string">User-defined card name</ResponseField>
        <ResponseField name="created_at" type="string">ISO 8601 timestamp</ResponseField>
        <ResponseField name="updated_at" type="string">ISO 8601 timestamp</ResponseField>
        <ResponseField name="card_limit" type="number">Maximum card balance limit</ResponseField>
        <ResponseField name="bin_id" type="string">Associated BIN identifier</ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="pagination" type="object">
      <Expandable title="properties">
        <ResponseField name="page" type="integer">Current page number</ResponseField>
        <ResponseField name="limit" type="integer">Items per page</ResponseField>
        <ResponseField name="total" type="integer">Total number of cards</ResponseField>
        <ResponseField name="totalPages" type="integer">Total pages available</ResponseField>
        <ResponseField name="hasNext" type="boolean">Whether more pages exist</ResponseField>
        <ResponseField name="hasPrev" type="boolean">Whether previous pages exist</ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="message" type="string">
  Success message
</ResponseField>

<RequestExample>

```bash cURL
curl -X GET "https://api.uncash.io/api/v1/cards?page=1&limit=10" \
  -H "Authorization: Bearer uc_your_api_token_here"
```

```javascript JavaScript
const response = await fetch('https://api.uncash.io/api/v1/cards?page=1&limit=10', {
  headers: {
    'Authorization': 'Bearer uc_your_api_token_here'
  }
});

const data = await response.json();
```

```python Python
import requests

url = "https://api.uncash.io/api/v1/cards"
headers = {"Authorization": "Bearer uc_your_api_token_here"}
params = {"page": 1, "limit": 10}

response = requests.get(url, headers=headers, params=params)
data = response.json()
```

</RequestExample>

<ResponseExample>

```json Response
{
  "success": true,
  "data": {
    "data": [
      {
        "id": 123,
        "card_nickname": "My Business Card",
        "created_at": "2025-08-03T10:00:00.000Z",
        "updated_at": "2025-08-03T10:00:00.000Z",
        "card_limit": 1000.00,
        "bin_id": "6705952d3ec949063b9241a0"
      }
    ],
    "pagination": {
      "page": 1,
      "limit": 10,
      "total": 25,
      "totalPages": 3,
      "hasNext": true,
      "hasPrev": false
    }
  },
  "message": "User cards retrieved successfully"
}
```

</ResponseExample>